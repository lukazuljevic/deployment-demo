export enum AppPaths {
  REGISTER = "/register",
  LOGIN = "/login",
  HOME = "/",
  PRODUCTS = "/products",
  FAVORITES = "/favorites",
  PROFILE = "/profile",
  ADMIN = "/admin",
  FORBIDDEN = "/forbidden",
  NON_EXSTING = "*",
}
